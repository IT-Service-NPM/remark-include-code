# Trim final newline

You can trim final newline with `trimFinalNewline` attribute.

Source files:

main.md:

::include-code{file="fixtures/main.md" language="markdown" trimFinalNewline=false}

included1.ts:

::include-code{file="fixtures/included1.ts" language="typescript" trimFinalNewline=false}

Remark output:

::include-code{file="snapshots/output.md" language="markdown" trimFinalNewline=false}

And You can trim final newline for all `::include-code` directives with
remark settings without `trimFinalNewline` attribute.

Remark settings (.remarkrc.mjs):

::include-code{file="./.remarkrc.mjs" language="javascript"}

or without config file:

::include-code{file="./example-with-settings.ts" language="typescript"}

> [!IMPORTANT]
>
> Package presets `remarkIncludeCodePreset` and `remarkIncludePresetSync`
> enables `trimFinalNewline` setting by default.
