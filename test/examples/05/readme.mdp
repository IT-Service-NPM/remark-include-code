# Trim final newline

You can trim final newline with `trim-final-newline` attribute.

Source files:

main.md:

::include-code{file="fixtures/main.md" language="markdown" trim-final-newline=false}

included1.ts:

::include-code{file="fixtures/included1.ts" language="typescript" trim-final-newline=false}

Remark output:

::include-code{file="snapshots/output.md" language="markdown" trim-final-newline=false}

And You can trim final newline for all `::include-code` directives with
remark settings without `trim-final-newline` attribute.

Remark settings (.remarkrc.mjs):

::include-code{file="./.remarkrc.mjs" language="javascript"}

or without config file:

::include-code{file="./example-with-settings.ts" language="typescript"}

> [!IMPORTANT]
>
> Package presets `remarkIncludeCodePreset` and `remarkIncludePresetSync`
> enables `trim-final-newline` setting by default.
